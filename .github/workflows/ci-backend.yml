name: Bibliophilarr Backend CI

on:
  pull_request:
    paths:
      - 'src/**'
      - '.github/workflows/ci-backend.yml'
  push:
    branches:
      - main
      - develop
      - staging
      - release
      - hotfix
    paths:
      - 'src/**'
      - '.github/workflows/ci-backend.yml'

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Legacy Brand Audit
        run: |
          # Strict zero enforcement is active only on dedicated rename lanes.
          if [[ "${GITHUB_REF_NAME}" == rename/* ]]; then
            echo "Running strict legacy brand audit for rename lane"
            ../scripts/legacy-brand-audit.sh --output ../_artifacts/legacy-brand-audit --enforce-zero
          else
            echo "Running visibility-only legacy brand audit"
            ../scripts/legacy-brand-audit.sh --output ../_artifacts/legacy-brand-audit
          fi

      - name: Upload Legacy Brand Audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: legacy-brand-audit
          path: _artifacts/legacy-brand-audit

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
          global-json-file: global.json

      - name: Restore
        run: dotnet restore Readarr.sln -p:Platform=Posix

      - name: Build
        run: >-
          dotnet build Readarr.sln --configuration Release --no-restore
          -p:Platform=Posix

      - name: Test
        run: |
          dotnet test NzbDrone.Common.Test/Readarr.Common.Test.csproj \
            --configuration Release \
            --no-build \
            -p:Platform=Posix \
            --verbosity minimal

          dotnet test NzbDrone.Host.Test/Readarr.Host.Test.csproj \
            --configuration Release \
            --no-build \
            -p:Platform=Posix \
            --verbosity minimal

          dotnet test NzbDrone.Api.Test/Readarr.Api.Test.csproj \
            --configuration Release \
            --no-build \
            -p:Platform=Posix \
            --verbosity minimal
