# Security Drift Cleanup 2026-03-17

## What changed

This note starts the current security-drift cleanup pass using live Dependabot alert data and lockfile evidence from `yarn.lock`.

## Why it changed

The repository still has open frontend dependency alerts. Operators need a small-slice remediation sequence instead of an unbounded dependency refresh.

## Current alert inventory

The latest local triage run reports 8 open alerts:

| Package | Open alerts | Severity | First patched | Lockfile versions | Status |
|---|---:|---|---|---|---|
| `glob` | 1 | high | `11.1.0` | `7.2.3`, `11.0.0` | potentially unresolved |
| `immutable` | 1 | high | `4.3.8` | `4.3.7` | potentially unresolved |
| `minimatch` | 4 | high | `3.1.3` and later lines | `3.0.8`, `3.1.2`, `5.1.6`, `9.0.3`, `10.0.1` | potentially unresolved |
| `postcss` | 1 | medium | `8.4.31` | `6.0.23`, `8.4.47`, `8.4.48` | potentially unresolved |
| `serialize-javascript` | 1 | high | `7.0.3` | `6.0.2` | potentially unresolved |

Source of truth for this table:

- local report generated by `python3 scripts/dependabot_lockfile_triage.py --owner Swartdraak --repo Bibliophilarr --lockfile yarn.lock`
- markdown artifact at `_artifacts/security-drift/dependabot-triage.md`

## PR triage implication

- PR `#11` is a safe narrow slice for the `immutable` alert and also refreshes the direct `postcss` pin from `8.4.47` to `8.4.48`.
- PR `#11` does not address the broader `glob`, `minimatch`, or `serialize-javascript` chains.
- The existing alert set therefore needs a sequenced cleanup plan even if the `immutable` slice lands first.

## Recommended merge sequence

1. Land the narrow `immutable` and `postcss` lockfile refresh after lockfile validation.
2. Address the `serialize-javascript` chain by updating the webpack and terser toolchain in one isolated slice.
3. Address the `glob` and `minimatch` family together so the lockfile is normalized once instead of through repeated churn.
4. Re-run Dependabot triage after each merge and record whether alerts clear or require dependency-graph refresh.

## Validation checklist per slice

- run `yarn install`;
- run `yarn build`;
- capture the resulting `yarn.lock` delta;
- re-run `python3 scripts/dependabot_lockfile_triage.py`;
- confirm whether the targeted package now meets or exceeds the first patched version.

## Constraints

- Keep remediation slices small enough that operator rollback is obvious.
- Do not collapse unrelated dependency updates into a single broad sweep unless lockfile churn makes it unavoidable.
- Treat Actions-token Dependabot restrictions as reporting limitations only. Local authenticated triage remains the source of truth for cleanup planning.

## Mitigation and rollback

- If a dependency bump breaks the frontend build, revert only that slice and leave the rest of the remediation queue untouched.
- If a lockfile update changes more packages than expected, re-scope the slice before merge rather than forcing unrelated upgrades through.