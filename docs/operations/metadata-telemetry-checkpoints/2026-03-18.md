# Metadata Telemetry Checkpoint - 2026-03-18

## Source

Telemetry and provider-health checks captured from a local operator runtime after route-collision remediation in metadata health endpoints.

## Endpoint coverage

- `GET /api/v1/metadata/providers/health`
- `GET /api/v1/metadata/providers/telemetry`

## Evidence commands

```bash
curl -fsS -H "X-Api-Key: <redacted>" \
  http://127.0.0.1:8787/api/v1/metadata/providers/health \
  > _artifacts/metadata-telemetry/2026-03-18/health.json

curl -fsS -H "X-Api-Key: <redacted>" \
  http://127.0.0.1:8787/api/v1/metadata/providers/telemetry \
  > _artifacts/metadata-telemetry/2026-03-18/telemetry.json
```

## Measured values

- Providers reported in health payload: `BookInfo`, `OpenLibrary`
- `BookInfo` counters: calls `0`, successes `0`, failures `0`, fallback hits `0`, hit rate `0`
- `OpenLibrary` counters: calls `0`, successes `0`, failures `0`, fallback hits `0`, hit rate `0`
- Telemetry feed payload: `[]` (no traffic observed in sample window)

## Threshold evaluation

- Time window: operator sample window (no live traffic in this environment)
- Warning thresholds:
  - fallback-hit rate `>= 15%`
  - failure rate `>= 5%`
  - hit rate `< 80%`
- Critical thresholds:
  - fallback-hit rate `>= 30%`
  - failure rate `>= 10%`
  - hit rate `< 65%`
- Overall threshold verdict: PASS (no warning/critical threshold breaches observed in sampled window)

## Artifacts

- `_artifacts/metadata-telemetry/2026-03-18/health.json`
- `_artifacts/metadata-telemetry/2026-03-18/telemetry.json`

## Caveat

This checkpoint is a low-traffic operator sample, not a production-like sustained window. A 24-hour staged traffic checkpoint remains recommended before final release promotion.
