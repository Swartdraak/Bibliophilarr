# Install Test Snapshot - 2026-03-17

## Scope

Validation run from `feature/open-library-provider-2026-03-17` to establish initial local install evidence for develop-lane cadence.

## Environment

- OS: Linux
- .NET SDK: 8.0.419
- Node.js: v20.19.2
- Yarn: 1.22.22
- Docker: 26.1.5

## Commands Executed

```bash
./build.sh --backend -r linux-x64 -f net8.0
./build.sh --frontend
./build.sh --packages -r linux-x64 -f net8.0
```

Native install smoke:

```bash
# isolated appdata and startup
"_artifacts/linux-x64/net8.0/Bibliophilarr/Bibliophilarr" \
  /data=/tmp/bibliophilarr-install-test \
  /nobrowser /nosingleinstancecheck

curl http://127.0.0.1:8794/ping
curl -H "X-Api-Key: local-install-test-key" http://127.0.0.1:8794/api/v1/metadata/providers/health
curl -H "X-Api-Key: local-install-test-key" http://127.0.0.1:8794/api/v1/metadata/providers/telemetry
```

Docker smoke (branch fallback path):

```bash
# Branch currently lacks root Dockerfile, so local test used temporary Dockerfile
DOCKER_BUILDKIT=0 docker build -t bibliophilarr:local-install \
  -f _artifacts/install-test/2026-03-17/docker/Dockerfile.local .

docker run -d -p 18787:8787 --name bibliophilarr-local-test \
  bibliophilarr:local-install /data=/tmp/bibliophilarr-docker /nobrowser /nosingleinstancecheck

curl http://127.0.0.1:18787/ping
```

npm packaging surface:

```bash
npm pack
```

## Results

- Native: PASS (`ping=200`, `providers_health=200`, `provider_telemetry=200`)
- Docker: PASS (`ping=200` on host port `18787`)
- npm packaging: PASS (`bibliophilarr-1.0.0.tgz` produced)

Evidence files:

- `_artifacts/install-test/2026-03-17/native/result.json`
- `_artifacts/install-test/2026-03-17/docker/result.json`
- `_artifacts/install-test/2026-03-17/npm-pack-files.txt`

## Known Limitations

- Root repository `Dockerfile` is absent on this branch; Docker validation used a temporary local test Dockerfile under `_artifacts/install-test/2026-03-17/docker/`.
- Dedicated `npm/bibliophilarr-launcher` directory is not present on this branch; npm validation used the root package tarball path.

## Rollback / Mitigation Notes

- If install regression appears, fall back to last known green branch commit where:
  - native ping endpoint returns `200`
  - Docker runtime starts with sqlite dependencies present
  - npm tarball still builds
- Keep temporary Dockerfile usage limited to local validation; reintroduce canonical repository Dockerfile path before release promotion.
