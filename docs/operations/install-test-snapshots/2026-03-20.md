# Install test snapshot - 2026-03-20

## Scope

Deterministic native Linux package startup validation after TD-006 through TD-009 hardening.

## Environment

- OS: Linux
- .NET SDK: 8.0.419
- Node.js: v20.19.2
- Yarn: 1.22.22

## Commands executed

Clean generated outputs before rebuilding:

```bash
rm -rf _output/net8.0 _tests/net8.0 _artifacts/linux-x64
./build.sh --backend --frontend --packages --lint --framework net8.0 --runtime linux-x64
```

Native package smoke:

```bash
./_artifacts/linux-x64/net8.0/Bibliophilarr/Bibliophilarr \
  /data=/tmp/bibliophilarr-package-smoke-2026-03-20 \
  /nobrowser /nosingleinstancecheck

curl http://127.0.0.1:8787/ping
```

## Results

- Native Linux package startup: PASS
- Health check: PASS (`/ping = 200`)
- Overall matrix verdict: PASS

Evidence notes:

- `_artifacts/linux-x64/net8.0/Bibliophilarr/Bibliophilarr` started successfully without copying `UI` manually.
- `_tests/net8.0/linux-x64` remained test-only output and was not used for runtime startup.
- Search telemetry diagnostics endpoint is now available for packaged-runtime validation at `/api/v1/diagnostics/search/telemetry`.

## Operational impact and rollback

What changed:
- Documented the deterministic native Linux runtime package startup path separately from raw `_output` and test artifacts.

Why it changed:
- Operators were previously starting binaries from the wrong artifact tree, which made runtime validation and integration bootstrap behavior harder to reason about.

How to validate:
- Re-run the commands above from a clean checkout and confirm `curl http://127.0.0.1:8787/ping` returns `200`.

Rollback and mitigation:
- If package startup regresses, fall back to the previous passing install evidence in [2026-03-17.md](2026-03-17.md) while re-validating package contents and UI placement for the affected runtime.